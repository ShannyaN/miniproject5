// Dependencies
    // Header (connected to time and date)
    // Add project button
    // Remove button
    // Elements


// DATA
    // Create local storage
    // * The name of the project.
    //  The type of project (use a <select> drop-down).
    // The date the project is due (use the date input type).


// Functions
function startTimer() {
    // Create date and time, MUST update every second (build with day.js)-- setInterval(), abbreviated month, day, full year, and time (e.g. Jun 30, 2022 at 08:37:48 am).

}

function displayProjects() {
    // Create, Build, Place projects as table data
}
    // Update clock every second (build with Day.JS) --setInterval(), abbreviated month, day, full year, and time (e.g. Jun 30, 2022 at 08:37:48 am).
function loadSavedProjects() {
    // get projects from local storage
    // display them on the page
}

function addProjects() {
    
}
    //  



// User Interactions
    // Add: project button
    // Add: X (delete) button


// Initialization
    // start timer
    startTimer();
    // load saved projects
    loadSavedProjects();